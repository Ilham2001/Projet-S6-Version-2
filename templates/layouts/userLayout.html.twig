<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Accueil{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="/css/style.css">
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
            <script type="text/javascript" src="/js/clockScript.js"></script>
        {% endblock %}
    </head>
    <body>
        <header>
            <a href="{{ path('user_index') }}" id="titre_page"></i>Accueil</a>
            <nav>
                <ul class="nav_liens">
                    <li><a href="{{ path('user_questions') }}">Questions</a></li>
                    <li><a href="{{ path('user_evaluations') }}">Evaluations</a></li>
                    <li><a href="{{ path('user_thematiques_matieres') }}">Thematique/Matières</a></li>
                    <li><a href="{{ path('logout') }}">Se deconnecter</a></li>
                </ul>
            </nav>
        </header>
        {% block body %}{% endblock %}
    </body>
</html>
